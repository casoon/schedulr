[package]
name = "schedulr"
version = "0.1.0"
edition = "2024"
rust-version = "1.87"
description = "Scheduling framework (activity/resource/interval DSL) built on unifier"
license = "MIT"
repository = "https://github.com/casoon/schedulr"
readme = "README.md"
keywords = ["scheduling", "timetabling", "planning"]
categories = ["algorithms", "mathematics"]

[dependencies]
unifier = { version = "0.1.0", path = "../unifier" }
